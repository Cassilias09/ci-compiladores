A = 5;
B = 10;
R = 0;
C = 0;
{
    if (A > 0 && B == 10) {
        R = R + 5;
    }

    if (A == 10 && B < 5) {
        R = R + 1; 
    }

    if (A == 0 || B < 15) {
        R = R + 5; 
    }

    if (A > 50 || B < 0) {
        R = R + 1;
    }

    if (!(A > 10)) {
        R = R + 5;
    }

    if (!(B == 10)) {
        R = R + 1; 
    }
    
    if (!(A == 5) && (B > 5) || (B == 10)) {
        C = R; 
    } else {
        C = C + 1; 
    }
    return C;
}